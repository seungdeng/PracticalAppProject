<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingStart="@dimen/common_padding"
    android:paddingEnd="@dimen/common_padding"
    android:paddingTop="@dimen/common_padding"
    android:paddingBottom="@dimen/common_padding">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">
        <TextView
            android:id="@+id/li_txt_step"
            style="@style/basicTextView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:textSize="@dimen/list_item_step_font_size"
            android:textColor="@color/txtColorStep"
            android:textFontWeight="400"
            android:textAlignment="textStart"
            android:text=""/>
        <TextView
            android:id="@+id/li_txt_result"
            style="@style/basicTextView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_weight="2"
            android:textSize="@dimen/list_item_result_font_size"
            android:textColor="@color/txtColorResult"
            android:text=""/>

    </LinearLayout>

</LinearLayout>